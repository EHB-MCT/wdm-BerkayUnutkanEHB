# Transfer Swipe

A simple football transfer swipe app (👍 / 👎).
Tracks events per user (UID), stores them in MongoDB, and shows analytics in an admin dashboard.

## Run

1. Copy `.env.template` to `.env`
2. docker compose up --build

## Docs

- Sources: docs/sources.md
- AI log: docs/ai-log.md
